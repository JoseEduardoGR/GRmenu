[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "grmenu"
version = "2.1.3"
description = "Menu de navegacion por teclado para terminal en modo TTY crudo (flechas + Enter)"
readme = "README.md"
requires-python = ">=3.9"
license = "MIT"
authors = [
    { name = "GRcode", email = "gonzalezrosalesjoseeduardo@gmail.com" }
]
classifiers = [
    "Environment :: Console",
    "Programming Language :: Python :: 3",
    "Operating System :: POSIX",
]

[project.scripts]
GRmenu = "GRmenu:GRmenu._cli"

[tool.hatch.build.targets.wheel]
include = ["GRmenu.py", "example_template.py"]

[tool.hatch.build.targets.wheel.force-include]
"data" = "data"

[tool.hatch.build.targets.sdist]
include = [
    "GRmenu.py",
    "example_template.py",
    "README.md",
    "LICENSE",
    "pyproject.toml",
]

[tool.hatch.build.targets.sdist.force-include]
"../data" = "data"
